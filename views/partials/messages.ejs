<% if (errorMessage) { %>
    <div class="alert alert-danger" style="color: red; margin: 1em 0;">
        <%= errorMessage %>
    </div>
<% } %>

<% if (successMessage) { %>
    <div class="alert alert-success" style="color: green; margin: 1em 0;">
        <%= successMessage %>
    </div>
<% } %>
